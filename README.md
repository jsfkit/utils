# JSF utils

A set of utilities and constants aimed at making JSF, the JSON Spreadsheet Format, easier to 
work with.

## Installing

The library is provided as an ES6 module in an NPM package:

```sh
npm install @jsfkit/utils
```

## Documentation

API documentation can be found in [API.md](./API.md).
